# 100UP CRM — MVP Development & Deployment Plan

**Date:** 20 September 2026
**Posture:** MVP fast. Decide, ship, iterate. Fred pays for rework if rework is needed.

Fred has said "don't over-complicate" enough times that it is now a working
rule for this project, not a mood. This plan therefore **decides** everything
that can reasonably be decided and leaves exactly one question for Fred. Where
a decision could be wrong, the cost of being wrong is stated, so it can be
reversed knowingly rather than avoided indefinitely.

---

## 1. Repo state as at 2026-09-20

**Working tree is clean.** The three modified docs (Fred's written answers,
bug #10 status, wishlist W1 closure) were committed as `1ee84c9`.

**`main` is in sync with `origin/main`.** No stashes, no worktrees.

**Migration history is healthy.** All 37 local migrations match remote
one-for-one, and `supabase migration list --linked` authenticates fine — the
PAT is current and the project is **not** paused. Last DB activity was
2026-09-15, five days ago; the free-tier auto-pause threshold is seven, so
this plan's first DB change also resets that clock.

**Build and lint are clean.** `npm run build` succeeds (606 kB bundle; the
chunk-size warning is advisory). `oxlint` reports 11 warnings, all
`only-export-components` / `exhaustive-deps` noise. No errors.

### Stale branches — safe to delete, nothing to recover

| Branch | Verdict |
|---|---|
| `chore/wip-email-integration-and-design-docs` (local + remote) | Fully merged |
| `docs/crm-design-brief` (local + remote) | Fully merged |
| `origin/claude/supabase-agent-skills-3bj4m2` | Fully merged |
| `origin/claude/print-stock-take-pdf-nrtrhw` | **Superseded.** Parallel agent build of the stock-take sheet; the shipped version is `1061594`. Branch predates all nine September migrations and would delete them |
| `origin/cloudflare/workers-autoconfig` | **Superseded.** Adds a root `wrangler.jsonc`; `main` already has `app/wrangler.jsonc` and deploys. Also predates the September migrations |

Recommend deleting all five. Neither superseded branch carries anything `main`
doesn't already have in a better form.

### Sibling repos

- **`100up-tools`** — clean, nothing pending.
- **`BW-CRM`** — five untracked files (`BW-CRM.code-workspace`, four `docs/*.md`
  specs including the AI Utility Layer and Task Inbox). Different product,
  outside this plan, but it is unsaved work sitting in a working tree.

---

## 2. CLAUDE.md was materially stale — corrected 2026-09-20

`CLAUDE.md` says *"All six Quote Designer tools… appear as `StubPage`
placeholders"* and *"Assumptions data is in the database but has no editing
screen"*. **Both are now wrong**, and any agent starting a session reads them
as fact.

Actual state of the Quote Designer:

| Tool | State |
|---|---|
| Quick Estimate | **Built** — `QuickEstimatePage.tsx`, 328 lines |
| Calculator | **Built** — `CalculatorPage.tsx`, 355 lines, on `quoteEngine.ts` |
| Assumptions | **Built** — `AssumptionsPage.tsx`, 454 lines |
| Simulation | **Built** — `SimulationPage.tsx`, 457 lines |
| Daily Load Profile | **Built** — `DailyLoadProfilePage.tsx`, 180 lines |
| 3 Phase | Stub |
| Ground Mount BOM | Stub |

Four of six are done. That changed the shape of the remaining work; `CLAUDE.md`
now says so.

---

## 3. The one thing that was blocking an end-to-end quote — CLOSED

**The Calculator had no "Send system to CRM" button.**

The Link-quote modal tells the user, in so many words:

> In the calculator, press *Send system to CRM* (copies the quote JSON), then
> paste it here.

That button exists in `100UP_suite_V46.html` (the `100up-quote` payload is
built at line 2719). It was never ported to `CalculatorPage.tsx`. The receiving
half — parse, fuzzy-match to stock, set job value, apply — is fully built and
working in `modals.tsx`.

So the new app has a complete quote engine and a complete quote receiver, and
nothing joining them. **This is the MVP gate**, and it is also precisely what
Fred named as the next priority:

> then next we need to work on the calculators/estimators and how that
> transfers into customers/jobs

Helpfully, `createJob(name)` in `features/jobs/actions.ts` already creates the
customer and the job together, so the direct path was small.

**Closed by Sprint 1 below**, and with stock ids rather than name matching.

---

## 4. Plan

### Sprint 1 — Close the quote loop (target: 1–2 days)

**DONE 2026-09-20.**

| # | Work | Outcome |
|---|---|---|
| 1.1 | Port `Send system to CRM` to `CalculatorPage` | **Done** — "Copy quote for CRM" on each result card, payload built by the new shared `lib/quotePayload.ts` |
| 1.2 | Add **Create job from this quote** | **Done** — customer name → `createJob()` → BOM onto the job → "Open job". No clipboard round-trip |
| 1.3 | Remove both `[NOT PARITY-VERIFIED]` banners | **Done** — Calculator, Quick Estimate, both file headers and the `index.css` comment |
| 1.4 | Bug #11 — floors on the numeric inputs | **Done** — `min={1}` and a clamp on daily load, total panels, min/max panels; `min={0}` on ground panels, where none is legitimate |
| 1.5 | ~~Bug #9 — add `Deye AI-W5.1-10P1-AU-B` to `stocks`~~ | **Was already done.** Added by `20260811120001` at $1,900 on 2026-08-11; `bugs.md` had gone stale. Fred confirmed the $1,900 on 2026-09-20 |

**1.3 is a decision, not a task.** Fred wrote: *"in our notes it should be
marked accepted — while there are known bugs or things to resolve, within the
parameters tested and how Fred plans to use it, it works fine."* That is
sign-off. The formal ten-scenario gate in `docs/phase-b-parity-gate.md` is good
engineering and would cost 1–2 hours; Fred has decided not to buy it. **Mark
the gate ACCEPTED-BY-OWNER, record that it was accepted without the scenario
run, and move on.** The residual risk is bug #8 (optimise + ground silently
ignoring the forced inverter mode) — Fred confirmed on 2026-08-11 that no job
was underpriced by it, so the exposure is forward-only and the fix is carried
in the new engine anyway.

### v2 payload — the handoff carries stock ids

V46 could only emit part *names*, generated from templates, which
`normalizePart()` then fuzzy-matched back against `stocks.name`. A miss is
silent, and that is the central defect in the quote → stock path.

The rebuilt engine already knows which stock row each cost line **is**
(`CostLine.stockId`), so the v2 payload passes it through and the receiver
uses it directly. Name matching survives only as the fallback for a v1 payload
pasted out of V46. Anything quoted in the new app no longer goes near the
regex.

This is most of risk 3 in §7 retired for the new path, without touching the
configurator rearchitecture.

### Sprint 2 — Installers are read-only (target: done 2026-09-20)

**Superseded by Fred's email of 2026-09-20.** The plan's earlier Sprint 2 was
built from the unattributed installer block in `docs/2026-08-12-Fredupdate`,
which described installers ticking their own steps. Fred's email says the
opposite, at length and without ambiguity:

> do not over complicate ... in general only me or someone who manage project
> move customer through stages, install only need to see job description he
> can not move or change anything, installer have to call me

Then step by step — Install complete, CES submitted, Inspector review, Fixes
complete, CES received, Rebate submitted, Rebate received — "**ALL completed
by me**". Date Job Booked: "Installer can not do anything, installer only can
see job description nothing else." Installation in progress and the other
installer stages: "**NO**." Alerts and notifications: "**NO alerts**."

So the installer model is: **see the job, write your own notes, call Fred.**

| # | Work | Status |
|---|---|---|
| 2.1 | Every step becomes admin-only | **Done** — `20260920100001`. Four steps were installer-settable (`install_in_progress`, `install_complete`, `ces_submitted`, `fixes_complete`); all now false |
| 2.2 | Installer sees no progression controls | **Done** — the UI already derives Advance / Move back / date editing from `installer_can_set`, so the data change hides them. A line in their place says the office moves jobs on |
| 2.3 | ~~Split Inspector review into requested / completed~~ | **Dropped.** Fred does the whole inspector cycle himself and asked not to over-complicate. Splitting a step he alone operates adds a click and buys nothing |
| 2.4 | ~~Job ref auto-fill and lock~~ | **Deferred.** Came from the unattributed block, not from Fred. The ref already pre-fills as `JO-0000-YYYY`; locking it is admin polish, not MVP |
| 2.5 | ~~Date issued auto-set and lock~~ | **Deferred.** Same source, same reasoning |
| 2.6 | Notification triggers | **Deferred, not cancelled.** "NO alerts" means not yet — Vanessa's read is that Fred will want them later. Leave undeveloped rather than designed-out |

### Hidden, not reverted — and that is the point

Nothing was removed. `installer_can_set`, `set_step_date`, the per-step checks
inside `advance_job_stage` / `move_job_back`, and the UI that reads them are
untouched and still enforced in the database. Only the *data* changed.

That matters because the likely path is that Fred changes his mind. Vanessa's
read: *"he said one thing but meant another — but will realise he actually
does want what he originally asked for."* He has also given two different
installer counts (five, then one — possibly one team of five), and he may be
trimming scope to get delivery sooner rather than describing what he wants to
live with. The second-tenant plan
(`.cursor/plans/second_deployment_plan_39d55bf9.plan.md`) assumes installer
logins get developed further for a wider rollout.

So the day any of that turns around, it is **one `update` statement and an
unhide**, not a rebuild. Reverting the code would have thrown away work that
is already correct and already enforced.

### "NO alerts" — resolved 2026-09-20

**Scope: automated notifications only, and only for now.** Vanessa's read is
that Fred will want them later, so they stay **undeveloped rather than
designed out** — nothing is built, nothing is removed, and no decision is
baked in that would have to be unpicked.

**It does not cover the Needs attention panel or the pulsing dots**
(`2b0ff99`, `docs/pipeline-attention-design.md`). Those mirror V46 in most
respects, which is the behaviour Fred already works with. They stay.

### Also noted, not acted on

Fred: "Rebate received — ... actually this is stage when job is get closed",
plus "we may rename or remove some of them". That implies the separate
`job_closed` step may be redundant. Left alone until he says which ones.

### Sprint 3 — Go-live hardening (target: 2–3 days)

These are the real cutover blockers.

| # | Work | Why it blocks |
|---|---|---|
| 3.1 | **Bug #14 — installer read lockdown** | **DONE 2026-09-20** — `20260920110001`. 22 policies moved from `using (true)` to `using (private.is_admin())`; installers keep their parts list through the redacting `stocks_visible` view. **Still needs one live re-test** as an installer before a real account is issued |
| 3.2 | **Auth email off Supabase's throttled SMTP** | Invites and password resets will rate-limit or silently fail. The CyberPersons connector already exists — point Supabase Auth SMTP at it |
| 3.3 | Enter the API keys | The `email` and `anthropic` connectors are built and keyless. Ten minutes, but nothing sends or reads an invoice until it's done |
| 3.4 | **Rework `scripts/import_from_export.py`** | Out of date against the current schema. Hard blocker on moving Fred's real data across |
| 3.5 | `noindex` on `offgridcrm.100up.com.au` | Small, and embarrassing if missed |

### After cutover — Fred pays for these

3 Phase port · Ground Mount BOM port · panels-only quoting · the
Assumptions→product configurator rearchitecture
(`docs/quote-configurator-design.md`) · Gmail sync · automated notification
triggers · real cost capture on receipt (W2) · **quote → customer lookup and
the "send quote to customer" flow (W3)**.

**W3 is the known gap in what Sprint 1 just shipped.** "Create job from this
quote" always creates a *new* customer, so quoting an existing one twice
produces a duplicate. The better shape — send the quote to the customer, and
create the contact and job as a side effect of sending — needs the email
connector keyed up first, so it waits for Sprint 3. Detail in
`docs/feature-wishlist.md` W3.

**Deliberate call on 3 Phase and Ground Mount BOM:** they stay stubs through
cutover. V46 still opens in a browser and still does them. Cutting over on
single-phase while keeping V46 for the two remaining tools costs Fred nothing
he isn't already doing today, and it pulls the CRM into business use weeks
earlier. Revisit only if three-phase volume makes it painful.

---

## 5. Answered by Fred, 2026-09-20 — nothing outstanding

**Deye `AI-W5.1-10P1-AU-B` (single-phase 10 kW) is $1,900.** Confirmed. That
is the value already seeded by `20260811120001`, so no change was needed. The
10P3 (three-phase) remains $2,300, corrected separately in `20260811150001`.

Two things followed from it.

**1. `bugs.md` #9 was stale.** It still read "Open — add the product". The
product was added on 2026-08-11. Now marked Fixed.

**2. The Deye 10kW three-phase should never be quoted.** Fred's reasoning: a
three-phase inverter splits its rating across the phases, so a 10kW 3P gives
~3.33 kW per phase, which won't carry what an off-grid three-phase customer
expects to run. The Deye three-phase fallback is the **12kW** `AI-W5.1-12P3`;
his most recent build used Sigenergy only.

This turned out to be a divergence the rebuild was about to introduce rather
than a change to anything Fred uses. **V46 already agrees with him** — its
three-phase Deye path hardcodes 12kW in every mode (lines 2259 / 2283 / 2288 /
2295) and never selects the 10P3. But `system_config_inverters` is seeded by a
blanket join over every active inverter per brand (`20260811160001`), so the
new configurator had picked the 10P3 up as a three-phase tier that V46 would
never have offered.

Fixed in `20260920090001_deye_three_phase_12kw_only.sql`: the **tier** is
removed, the **`stocks` row stays** (real product, carries the CES catalogue
entry, needed for any historical job).

**There is no Deye 15kW.** The 15kW is Sigenergy's — `SigenStor EC 15.0 TP`,
the *small* of Sigenergy's three-phase range. Deye's AU three-phase range here
is 10P3 and 12P3 only.

### The trap this nearly set

Removing a tier isn't free. `quoteEngine.ts` filtered candidates hard on
`size_class === forceSizeClass` and returned `null` when nothing matched. With
the 10P3 gone, Deye three-phase has one tier against Sigenergy's three — so
forcing "small" or "medium" would have **silently blanked the Deye column**,
which is the exact failure the `size_class` design was written to prevent.

The engine now falls back to a config's remaining tiers for the phase when the
forced class matches none of them. That is V46's behaviour by construction:
the 12kW Deye answers every three-phase mode. Without this, porting 3 Phase
later would have hit an empty column with no obvious cause.

**On "just don't display Deye in 3-phase":** not taken. Fred named the 12kW as
the fallback and V46 offers it, so dropping Deye from three-phase entirely
would diverge from both. Removing one tier keeps the two brands comparable,
which is the point of the side-by-side screen.

**If Fred later wants a Deye 15kW, or any other tier:** one `stocks` row plus
one `system_config_inverters` row. Data entry, no code. That is what the
configurator design bought.

### Decided without asking — and why

| Question | Decision | Cost if wrong |
|---|---|---|
| Drop Deye from three-phase entirely, or just the 10kW tier? | **Just the 10kW tier.** Fred named the 12kW as the fallback and V46 offers it | Dropping Deye wholesale would diverge from V46 and lose the comparison column |
| Run the formal ten-scenario parity gate? | **No.** Fred accepted parity in practice | Bug #8 territory; he already confirmed no historical underpricing |
| Bug #10 — should the July simulation require sustainability (end-of-month SOC ≥ start)? | **No — closed by Fred, 2026-09-20.** Battery starts full and that is fine: internal tool, no guarantees given off it, it is a simulator. The V46 pass rule stands, and the "array cannot sustain this load" warning was already built | None — this is his answer, not an assumption |
| Bug #11 fix approach | **`min={1}` on numeric inputs** | Fred prescribed it himself |
| W1 — file supplier docs to Google Drive | **Closed.** Fred: "not needed" | None |
| Cut over before 3 Phase / GM BOM are ported? | **Yes** — dual-run V46 for those two | He dual-runs today regardless |
| Where does the lead stage live? | **Stage 1 / First Contact**, as it already does | Fred named it |

---

## 6. Sequencing and deploy

Deploy is push-to-`main`; Cloudflare Workers Builds rebuilds automatically.
Database changes go via `supabase db push` — never through an MCP connection
(convention 1; that has already broken the migration history once).

```
Sprint 1  ──► push ──► Fred can quote end-to-end in the new app
Sprint 2  ──► push ──► job/installer model matches what Fred asked for
Sprint 3  ──► push ──► safe for real installers and real data
                 └──► CUTOVER
```

Schema changes can still ship in one go — Fred is not yet depending on the app,
so no staged "deploy frontend, drop column later" dance is needed. **That flips
at cutover**, and Sprint 3 is the last sprint where it holds.

Regenerate `app/src/types/database.types.ts` after every migration.

---

## 7. Risks worth naming

1. **Bug #14 is the one that would actually hurt.** Fred's margin is readable
   by any authenticated installer. It isn't urgent while no installer has an
   account, and it becomes urgent the hour one does. Don't let Sprint 3 slip
   past the first installer invite.
2. **`import_from_export.py` is untested against the current schema.** The
   estimate above assumes rework, not a rewrite. Worth an hour of reading
   before committing to the Sprint 3 timeline.
3. **The pricing↔stock link is still a regex** (`normalizePart.ts`) for
   **V46-originated quotes only**. Quotes raised in the new app now carry
   `stockId` end to end (v2 payload) and never touch the matcher. Until
   cutover, V46 is still where quotes come from, so the risk is live — but it
   retires itself as the new Calculator takes over.
4. **Free-tier auto-pause.** Seven days without DB activity takes the whole
   stack down. Five days had already elapsed when this plan was written.
