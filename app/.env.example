# Copy to .env.local for local dev. In Cloudflare Pages these are set as
# build environment variables. The publishable key is safe client-side;
# RLS is the access-control boundary.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
