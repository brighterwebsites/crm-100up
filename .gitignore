# Supabase CLI local state — links, pooler URLs, cached remote versions.
# Machine-specific; regenerated by `supabase link`.
supabase/.temp/

# Environment
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
